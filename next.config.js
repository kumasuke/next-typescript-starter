/**
 * @see https://github.com/vercel/next.js/issues/10254
 * @type {import('next').Config}
 */
const config = {
  reactStrictMode: true,
  poweredByHeader: false,
  webpack5: true,
}

module.exports = config
